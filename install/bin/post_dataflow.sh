#!/bin/bash

DATAFLOW=$1

echo "START POST DATAFLOW $DATAFLOW"

POST_DATAFLOW_TIME=($($PRECISION100_FOLDER/bin/audit.sh  $0 "POST-DATAFLOW" "$PROJECT_FOLDER / $EXECUTION_NAME / $OPERATION / $SIMULATION_MODE / $DATAFLOW" "$PROJECT_FOLDER / $EXECUTION_NAME" "DATAFLOW" $DATAFLOW "END"))

declare PRE_DATAFLOW_TIME
echo ${POST_DATAFLOW_TIME[2]}

echo "END POST DATAFLOW $DATAFLOW"
