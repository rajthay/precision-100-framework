#!/bin/bash

DATAFLOW=$1

echo "START PRE DATAFLOW $DATAFLOW"

PRE_DATAFLOW_TIME=($($PRECISION100_FOLDER/bin/audit.sh  $0 "PRE-DATAFLOW" "$PROJECT_FOLDER / $EXECUTION_NAME / $OPERATION / $SIMULATION_MODE / $DATAFLOW" "$PROJECT_FOLDER / $EXECUTION_NAME" "DATAFLOW" $DATAFLOW "START"))

declare PRE_DATAFLOW_TIME
echo ${PRE_DATAFLOW_TIME[2]}
echo "END PRE DATAFLOW $DATAFLOW"
